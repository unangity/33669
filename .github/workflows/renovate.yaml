name: Renovate
on:
  pull_request:

jobs:
  renovate:
    name: Run renovate
    runs-on: ubuntu-latest
    steps:
      - name: checkout
        uses: actions/checkout@v4
      - name: Renovate
        uses: renovatebot/github-action@v41.0.13
